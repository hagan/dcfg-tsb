# macOS platform defaults (Intel and Apple Silicon)

platform="darwin"

# macOS-specific module defaults
module.gpg_agent="on"
module.theming="auto"

# Homebrew paths differ by architecture
# Intel: /usr/local
# ARM: /opt/homebrew
